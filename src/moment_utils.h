#ifndef MOMENT_LIB_MOMENT_UTILS_H
#define MOMENT_LIB_MOMENT_UTILS_H

/*
 * NOTE: 这里声明工具函数
 */

#include <iostream>
#include <regex>
#include <string>

namespace moment_lib {
namespace utils {

time_t stod(std::string s);

}  // namespace utils
}  // namespace moment_lib

#endif
